namespace Outboxer.Config;

internal static class EntryDefaultConfig
{
    /// <summary>
    /// Default value for message delivery retry-attempt
    /// </summary>
    internal const int DEFAULT_RETRIES = 5;
}